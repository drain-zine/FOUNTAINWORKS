import React from "react";

const HomePage = () => {
    return(
        <main>
            <section>
                <SplashImage/>
            </section>
        </main>
    );
};

export default HomePage;